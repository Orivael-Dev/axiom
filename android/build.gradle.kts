// Top-level build file — versions are centralised here so child modules
// reference them via the version catalog or by string. Pinned to a known-
// stable AGP + Kotlin pair (Android Gradle Plugin 8.2.x + Kotlin 1.9.22).

plugins {
    id("com.android.application") version "8.2.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.22" apply false
    id("org.jetbrains.kotlin.plugin.serialization") version "1.9.22" apply false
}
