<?xml version="1.0" encoding="utf-8"?>
<!--
    Only the Android-emulator → host loopback (10.0.2.2) and standard
    localhost may use cleartext HTTP. Every other destination requires
    TLS. This narrows the cleartext window to dev-only traffic without
    weakening production calls.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
