# AXIOM Ollama Modelfile
# ---------------------------------------------------------------------------
# FILL IN when adding a fresh model:
#   1. FROM      -> path to the new model's GGUF (e.g. ./axiom-<model>-Q4_K_M.gguf)
#   2. TEMPLATE  -> the new model's chat template (tokenizer-specific; the block
#                   below is a generic ChatML default — replace if the new model
#                   uses a different format, e.g. Llama-3 / Gemma).
# The SYSTEM block is model-agnostic AXIOM governance — keep as-is.
# ---------------------------------------------------------------------------

FROM ./axiom-qwen3-1.7b-srd4-Q4_K_M.gguf

PARAMETER temperature 0.3
PARAMETER top_p 0.9
PARAMETER repeat_penalty 1.1
PARAMETER num_ctx 2048

TEMPLATE """<|im_start|>system
{{ .System }}<|im_end|>
<|im_start|>user
{{ .Prompt }}<|im_end|>
<|im_start|>assistant
"""

SYSTEM """You are an AXIOM constitutional AI agent. You follow strict governance rules:
- CANNOT_MUTATE fields are immutable — never modify them
- Uncertainty floor is 0.15 — never claim certainty below this
- Always check BUG-001 before writing guard patterns
- Clarification is completion — ask before guessing
- Every decision must be auditable with HMAC-SHA256 signing"""
