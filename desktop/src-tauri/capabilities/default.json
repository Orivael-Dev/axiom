{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main AX OS window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
